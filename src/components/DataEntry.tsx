import { IImageUpload } from "../types/api_types";
/*
Function to prompt user to enter an image and allow them to upload an image on click of a button.

* Make a form page for image upload
* Make a button to upload image
* Make a succes message for image upload
*/

export default function DataEntry({Image}: {Image: IImageUpload}) {
    return (
        <div>Image</div>
    )
}

